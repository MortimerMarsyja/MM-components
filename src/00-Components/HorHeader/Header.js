import React from "react";
import HorStyledHeader from "./HorHeader.style";

const HorHeaderComponent = ({ children }) => {
  return <HorStyledHeader>{children}</HorStyledHeader>;
};

export default HorHeaderComponent;
